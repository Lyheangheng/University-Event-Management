==============================================================================
PHASE 5 REPORT: STUDENT EVENT UI
==============================================================================

1. Phase Name:
   Phase 5 — Student Event UI

2. Completion Date/Time:
   2026-09-19T21:00:30+07:00 (Asia/Bangkok)

3. Frontend Architecture:
   - Next.js (App Router), React, TypeScript, Tailwind CSS.
   - Centralized API client at apps/web/src/lib/api.ts consuming backend REST endpoints.
   - Responsive component layout matching existing dark mode glassmorphism theme.
   - Dedicated helper functions for date, time, and display status formatting.

4. Files/Components Created:
   - apps/web/src/types/event.ts
   - apps/web/src/lib/api.ts
   - apps/web/src/lib/formatters.ts
   - apps/web/src/components/EventCard.tsx
   - apps/web/src/components/SkeletonCard.tsx
   - apps/web/src/app/events/page.tsx
   - apps/web/src/app/events/[id]/page.tsx
   - apps/web/src/app/page.tsx (updated with navigation link)

5. Routes Created:
   - /events         (Student Event List Page)
   - /events/[id]    (Student Event Detail Page)

6. API Endpoints Consumed:
   - GET /api/events
   - GET /api/events/:id

7. Event Data Displayed:
   - id, title, description, date, startTime, endTime, location, targetGroup, imageUrl, createdAt, updatedAt.

8. Event Status Logic:
   - UPCOMING: Current time < startTime (Indigo badge)
   - ONGOING: Current time >= startTime AND Current time < endTime (Emerald pulse badge)
   - ENDED: Current time >= endTime (Slate badge)
   - Calculated dynamically on frontend for display purposes only.

9. Loading / Empty / Error States:
   - Loading: Grid of 3 animated SkeletonCard components.
   - Empty: Friendly "No events available" card when event array is empty.
   - Error: User-friendly error card with "Retry" action button.
   - 404 Detail: "Event Not Found" error card with "Back to Events" navigation link.

10. Responsive Design Implementation:
    - Tailwind grid (1 column mobile, 2 columns tablet, 3 columns desktop).
    - Mobile-friendly tap targets and layout optimized for future LINE LIFF webview integration.

11. Image Handling:
    - Displays event image when imageUrl is present and valid.
    - Graceful fallback gradient icon placeholder when imageUrl is null/empty or triggers an onError event.

12. Environment Variables Added:
    - NEXT_PUBLIC_API_URL (default: http://localhost:3001). Updated in .env.example and .env.

13. Testing Performed:
    - Next.js production build (next build): PASSED (5 pages prerendered cleanly).
    - Status calculation logic verification.
    - Route rendering and navigation checks (/events -> /events/[id] -> /events).

14. Test Results:
    - PASSED (0 compilation errors).

15. Manual Configuration Required:
    - Ensure backend NestJS server (apps/api) is active on port 3001 or configure NEXT_PUBLIC_API_URL.

16. Dependencies Added:
    - None (reused existing frontend dependencies).

17. Problems Encountered:
    - None.

18. Deviations from Requirements:
    - None.
==============================================================================
